# 面试 Agent 场景题设计规范
## SPEC: Scenario Question Design & Interviewer Evaluation Logic

> 目标：定义场景题的产品逻辑——AI 面试官在追问什么、评价什么、如何判断峰值表现。
> 范围：场景题（40% 权重）的题库结构 + 追问链设计 + 评分维度 + 峰值定理应用规则。
> 状态：**草稿 v0.1**，待与面试者（用户）确认后定稿。

---

## 1. 背景与核心假设

### 1.1 场景题的本质

算法题和八股题有标准答案或固定范围，考察的是"知识储备"和"思维正确性"。
场景题考察的是"真实能力"，包括：

- 知识能不能拿来用（而不是背下来）
- 遇到新问题能不能快速找到关键矛盾
- 在压力下能不能讲清楚自己的思路
- 能不能评估方案的 trade-off（而不是只知道最优解）

### 1.2 面试官的两个角色

在场景题中，面试官同时扮演两个角色：

| 角色 | 行为 | 目的 |
|------|------|------|
| **好奇心驱动者** | 顺着候选人的回答追问"为什么"和"然后呢" | 探测候选人能力的水位上限 |
| **质量把关者** | 评估回答的准确性、深度、trade-off 意识 | 决定是否通过 |

AI 面试官也要同时扮演这两个角色。Phase 1 先实现"好奇心驱动者"，Phase 2 加入"质量把关者"（即 gate）。

### 1.3 候选人的两种回答模式

| 模式 | 特征 | AI 行为 |
|------|------|---------|
| **宽泛但浅** | 回答覆盖很多点，但每个点都只到表层 | 追问链：选最值得深挖的点继续问 |
| **窄但深** | 回答集中在一点，挖得比较透 | 横向扩展：问这个方案 vs 其他方案，或者问边界情况 |
| **答非所问** | 没有回应问题本身 | 重新表述问题，若仍不回答则标记 weak_point |
| **主动扩展** | 主动提了自己知道但问题没问到的点 | 记录为"潜在亮点"，可顺着她追问 |

---

## 2. 场景题题库结构

### 2.1 两层分类

场景题只有两层分类，**不做更细的垂直拆分**（Kafka 和 RocketMQ 都属于"消息队列"，不需要分开处理）：

| 分类 | 覆盖领域 | 示例话题 |
|------|---------|---------|
| **Foundation** | 多线程/并发、异步、缓存、消息队列、分布式基础、性能分析 | 连接池配置、缓存穿透、消息顺序、SQL 优化 |
| **Business** | 订单流程、库存管理、供应商集成、价格计算、状态机设计 | 超卖问题、预加载策略、价格一致性、供应商压测 |

### 2.2 题目结构（scenario question schema）

```json
{
  "id": "scenario-foundation-001",
  "pool": "scenario",
  "scenario_type": "foundation",
  "category": "缓存",
  "difficulty": "medium",
  "seed": "你们的预加载策略是怎么设计的？什么条件下会触发刷新？",
  "context": "某酒店预订平台，支持 300+ OTA，日均请求 5000 万，命中率约 67%。团队正在优化预加载策略。",
  "knowledge_anchors": [
    "按房间类型+入住日期分区加载",
    "TTL + 主动 invalidate 双机制",
    "与 CDN 预热配合",
    "热点数据识别（访问频次 > N 次/分钟）"
  ],
  "depth_trigger": "候选人提到具体刷新条件（比如 '每天 0 点全局刷新' 或 '按房间状态机触发'）",
  "followup_probes": [
    {
      "trigger": "候选人提到全局刷新",
      "probe": "全局刷新会不会造成雪崩？有没有预热机制？",
      "differentiating_value": "HIGH"
    },
    {
      "trigger": "候选人提到 TTL",
      "probe": "TTL 设多少？怎么平衡命中率与数据新鲜度？",
      "differentiating_value": "LOW"
    }
  ],
  "expected_depth": 3,
  "typical_rounds": "3-5"
}
```

其中 `followup_probes` 是可选的初始追问种子，正式面试中的追问由 AI 根据回答动态生成。

### 2.3 题目数量目标

| 池 | 目标数量 | 来源 |
|---|---------|------|
| Foundation | >= 30 | 作者自写（基于 jproduct 经验 + 公开面经整理） |
| Business | >= 20 | 作者自写（来自真实业务场景） |

---

## 3. 面试官的追问链设计

### 3.1 追问的方向

每次追问链向以下五个方向展开，AI 根据候选人上一轮回答决定往哪个方向追：

| 方向 | 典型问题句式 | 考察什么 |
|------|------------|---------|
| **往深度走** | "能展开讲讲吗？"、"这个机制具体怎么实现的？" | 技术细节掌握程度 |
| **往 trade-off 走** | "这样做的缺点是什么？"、"有没有 alternative 方案？" | 方案权衡思维 |
| **往边界情况走** | "如果这个参数设成 0 会怎样？"、"并发量再大 10 倍呢？" | 应变能力 |
| **往业务影响走** | "这个改动对 OTA 的 SLA 有什么影响？"、"用户感知是什么？" | 业务 sense |
| **往协作走** | "你怎么推动团队接受这个方案？"、"如果 PM 不同意怎么办？" | 沟通与影响力 |

### 3.2 追问链的形成规则

**初始题** → 候选人回答 → AI 从五个方向中选最值得追问的一个追问 → 下一轮

继续追问的条件：
- `followup_count < 10`（全局上限）
- 当前话题"还没问透"（AI 自己判断：是否覆盖了 >= 2 个 knowledge_anchors）
- 候选人在认真回答（不是"这个我不太清楚"直接放弃）

**提前终止的条件：**
- 候选人连续两轮表示不确定/不知道 → 标记 weak_point，停止追问
- AI 判断"这个问题已经问透了"（知识锚点都覆盖了）
- `followup_count >= 10` → 全局停止

### 3.3 追问的自然感

追问要像真实面试官，不是审讯。关键原则：
- **不重复**：不问候选人刚说过的内容
- **不跳跃**：不跳到完全无关的话题（除非候选人主动延伸）
- **给肯定**：候选人答得好的点，可以简短肯定后继续追问
- **追问而不是批评**：不用"你错了"，而是用"如果考虑 XXX 情况呢？"来提问

---

## 4. 评分维度

### 4.1 四个评分维度

| 维度 | 含义 | 满分标准 |
|------|------|---------|
| **clarity** | 表达清晰度 | 能把技术方案讲清楚，非技术人员也能听懂大意 |
| **depth** | 技术深度 | 能把原理和细节讲清楚，不是只会用不懂原理 |
| **tradeoffs** | 权衡思维 | 能分析方案的优劣，能说清楚为什么选 A 不选 B |
| **honesty** | 诚实度 | 不懂的不瞎编，能说"这个我不太确定" |

### 4.2 每轮评分规则

每轮回答后，AI 给出 ScoreDelta：

```
score_delta = {
  "dimension": "clarity | depth | tradeoffs | honesty",
  "value": +1.0 ~ +3.0,   # 正面回答
  "reason": "一句话说明为什么加/减分"
}
```

加分上限：clarity/depth/tradeoffs 最高 +3/轮，honesty 最高 +2/轮。
扣分：答非所问或瞎编 → 对应维度 -1.0。

### 4.3 峰值定理应用规则（Scenario Layer）

峰值只在以下条件同时满足时触发：

1. AI 生成了 follow-up（不是初始问题）
2. AI 给这个 follow-up 的 `differentiating_value = HIGH`
3. 候选人接住了这个 follow-up（honesty 不扣分）

触发时：scenario_score 额外 +5。

**错误示范：** AI 自己出一道 LOW DV 的追问，候选人也答对了，但不该给峰值加分（峰值只奖励真正有区分度的表现）。

---

## 5. 典型追问链案例

### 案例：缓存穿透场景

```
Q: 你们的预加载策略是怎么设计的？
A: 我们是按房间类型+入住日期分区加载，每天 0 点全局刷新。

→ 追问1（往 trade-off 走）:
  "全局刷新会不会造成雪崩？有没有预热机制？"
  → A: "我们用了双缓冲，先预热新数据再切换。"
  → 追问2（往深度走）:
    "双缓冲的切换时机怎么定？有没有检测数据有效性的机制？"
    → A: "我们有个 health check 定时任务..."
    
→ 如果候选人接住追问2：peak +5
→ 如果候选人说"不太清楚"：honesty 扣分，提前终止
```

### 案例：超卖场景（Business）

```
Q: 库存超卖了怎么办？
A: 我们用了分布式锁，乐观锁版本号控制并发更新。

→ 追问1（往边界情况走）:
  "如果 Redis 锁和 MySQL 版本号判断出现 race condition 怎么办？"
  → A: "我们加了兜底逻辑，每次下单前再查一次 MySQL 的实时库存。"
  
→ 追问2（往业务影响走）:
  "这个兜底逻辑的 RT 增加多少？对携程这种大 OTA 的下单成功率有影响吗？"
  → 如果候选人能说出具体数据和业务影响：peak +5
```

---

## 6. 待确认问题

- [ ] 追问链上限 10 次是否合理？（如果问得太深会不会超时）
- [ ] `differentiating_value = HIGH` 的判断标准是否能通过 Prompt 稳定复现（需要 Phase 2 验证）
- [ ] 场景题题库的初始规模是否足够？（30 foundation + 20 business，够不够覆盖常见话题）
- [ ] 候选人主动说的亮点怎么记录？要不要设计一个单独的"亮点池"字段？

---

*Last updated: 2026-05-26*
*Author: Liu Hanqi*
*Status: 草稿，待确认*